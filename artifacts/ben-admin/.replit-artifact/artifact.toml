kind = "web"
previewPath = "/"
title = "BenAdmin"
version = "1.0.0"
id = "artifacts/ben-admin"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18896

[services.development]
run = "pnpm --filter @workspace/ben-admin run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/ben-admin", "run", "build" ]
publicDir = "artifacts/ben-admin/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18896"
BASE_PATH = "/"
